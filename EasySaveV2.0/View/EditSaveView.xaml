<Window x:Class="EasySaveV2._0.EditSaveView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EasySaveV2._0"
        mc:Ignorable="d"
        Title="AddSaveView" Height="450" Width="800">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBackground" Color="#F7F7FF"/>
        <SolidColorBrush x:Key="ButtonBackground" Color="#FF007ACC"/>
        <SolidColorBrush x:Key="ButtonForeground" Color="White"/>
        <Style x:Key="TabItemBackgroundStyle" TargetType="TabItem">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush EndPoint="0,1">
                        <GradientStop Color="#FFF0F0F0"/>
                        <GradientStop Color="#FFE5E5E5" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ButtonBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource ButtonForeground}"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Margin" Value="5"/>
        </Style>
    </Window.Resources>
    <Grid Margin="20 10" MaxWidth="600">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="24" Text="{Binding viewModel.UI.TextBoxEditSave}"/>
        <Label Grid.Row="1" Content="{Binding viewModel.UI.LabelNameSave}"/>
        <TextBox Grid.Row="2" Name="txtBackupName" Margin="5" Text="{Binding viewModel2.SelectedItem.saveName}"/>

        <Label Grid.Row="3" Content="{Binding viewModel.UI.LabelSourceSave}"/>
        <TextBox Grid.Row="4" Name="txtSourceFile" Margin="5" Text="{Binding viewModel2.SelectedItem.sourcePath}"/>

        <Label Grid.Row="5" Content="{Binding viewModel.UI.LabelSaveTarget}"/>
        <TextBox Grid.Row="6" Name="txtDestinationFile" Margin="5" Text="{Binding viewModel2.SelectedItem.targetPath}"/>

        <Label Grid.Row="7" Content="{Binding viewModel.UI.LabelType}"/>
        <ComboBox Grid.Row="8" Name="cmbTypeFile" Margin="5" SelectedItem="{Binding viewModel2.SelectedItem.fileType}">
            <ComboBoxItem Content="Complet"/>
            <ComboBoxItem Content="Différentiel"/>
        </ComboBox>

        <Button Grid.Row="9" Content="{Binding viewModel.UI.EditSaveButtonText}" Margin="5" Style="{StaticResource ActionButtonStyle}" Command="{Binding viewModel2.UpdateSaveCommand}"/>
    </Grid>
</Window>
